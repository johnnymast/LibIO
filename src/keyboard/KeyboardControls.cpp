#include "LibIO/keyboard/KeyboardControls.hpp"

namespace LibIO::Keyboard {
    void KeyboardControls::PressKey(const std::string &key) {
        // Needed definition so it woill ve in the vtable
    }

    void KeyboardControls::Hotkey(const std::string &modifier, const std::string &key) {
        // Needed definition so it woill ve in the vtable
    }
}

#include "LibIO/mouse/MouseControls.hpp"

namespace LibIO::Mouse {
    void MouseControls::ClickButton(int button) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::MoveCursor(int x, int y) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::LeftClick() {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::RightClick() {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::MoveAndLeftClick(int x, int y) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::MoveAndRightClick(int x, int y) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::Scroll(int amount, int *x, int *y) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::ScrollUp(int amount) {
        // Needed definition so it woill ve in the vtable
    }

    void MouseControls::ScrollDown(int amount) {
        // Needed definition so it woill ve in the vtable
    }
}
